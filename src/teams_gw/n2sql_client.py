"""
Legacy module preserved for backward compatibility with previous releases that
spoke with the N2SQL backend. The Teams In/Out template answers directly and
uses proactive messaging instead, so the HTTP client has been intentionally
removed.
"""
