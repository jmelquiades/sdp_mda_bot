"""
Legacy module preserved for backward compatibility.

The original project converted N2SQL responses into Markdown tables. The new
Teams In/Out template no longer needs that logic, but we keep the file (empty on
purpose) so that imports from older branches fail fast if they accidentally
survive the refactor.
"""
